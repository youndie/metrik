package ru.workinprogress.metrik.agent

/**
 * Рантайм, в котором крутится агент.
 *
 * Дашборду это нужно знать, а не выводить: «heap» на JVM и «RSS» на нативе — разные величины, и
 * подписать их одинаково значит соврать. Единственный признак, по которому раньше угадывали
 * (отсутствие максимума heap), в контейнере не работает — там нативный агент кладёт туда лимит
 * cgroup.
 */
internal expect val agentRuntime: String
