# metrik — бэклог

Лёгкий мониторинг Ktor-сервисов: плагин-агент, нативный сервер с SQLite, дашборд на Compose Wasm.
Архитектурные решения и обоснования — [docs/research/research-architecture.md](docs/research/research-architecture.md).
Читать перед тем, как брать любую задачу.

**Статус: кода нет.** Всё в `docs/` описывает целевую архитектуру (`status: draft`), а не то, что
существует. Первое, что делает M0, — заводит репозиторий, после чего действует обычный инвариант
docs-подхода: `main` описывает то, что есть.

## Milestone-цель

**M1–M4 = минимально полезный продукт:** подключил плагин к своему Ktor-сервису → через минуту
видишь RPS, ошибки и p95 в дашборде. Всё остальное — после того, как это работает на живом сервисе.

---

## M0 — каркас репозитория ✅

- [x] **M-01** Gradle-проект: `:shared`, `:agent`, `:server`, `:composeApp` (не `:web` — модуль
      сразу Compose-овый, с desktop-таргетом для быстрой отладки UI). Kotlin 2.4.10, Ktor 3.5.2
      через `ktorLibs`, sqlx4k 1.13.0, ktlint 1.8.0.
- [x] **M-02** CI: `./gradlew build` (включает `ktlintCheck` и тесты) на PR и push в main,
      с кэшем `~/.konan`.
- [x] **M-03** `CLAUDE.md` в корне: модули, таргеты, команды, форматирование, референсы.

Сверх плана в M0 попал минимальный рабочий сервер: `ServerConfig` с `expect/actual` чтением env
(на нативе `System.getenv` нет) и роут `/health`. Это не забег вперёд по M3, а единственный способ
доказать, что тулчейн собран правильно: нативный бинарь линкуется, Ktor CIO на нативе стартует,
конфиг читается. Проверено — бинарь поднимается за 1 мс, `/health` отвечает 200, без
`METRIK_INGEST_KEY` падает намеренно.

## M1 — протокол и общий код

- [ ] **M-10** `:shared`: модель `Frame`/`RouteSeries`/`SystemSnapshot`/`SlowSample` по
      [protocol-ingest](docs/api/protocol-ingest.md), `ignoreUnknownKeys = true`.
- [ ] **M-11** Кодек гистограммы: `durationToBucket`, `bucketBounds`, `percentile(sparse, q)`,
      сложение гистограмм. **Тесты обязательны** — это единственное место, где ошибка
      незаметно испортит все цифры продукта.
- [ ] **M-12** Нарезка окна на пакеты ≤ 1200 байт (`q`/`n`), тест на серию, не влезающую в пакет,
      и на окно из 300 серий.

## M2 — агент

- [ ] **M-20** `install(Metrik)`: `createApplicationPlugin`, конфиг, хуки `Metrics` / `ResponseSent`
      / `CallFailed`; шаблон маршрута из `RoutingRoot.routingCallKey`.
- [ ] **M-21** `WindowAggregator`: окно, атомарная ротация, лимит `maxSeries` → `<other>`,
      top-N медленных.
- [ ] **M-22** `expect/actual` системных метрик: JVM (`Runtime` + `ProcessHandle`, опциональный
      JMX через ленивую проверку), Linux native (`/proc/self/stat`, `/proc/self/statm`).
      См. [feature-system-metrics](docs/features/feature-system-metrics.md) — раскладка по Native
      там пока гипотеза, задача включает её проверку.
- [ ] **M-23** Бенчмарк горячего пути: цена замера на запрос, цель ≤ ~100 нс, и подтверждение,
      что при недоступном сервере латентность не растёт.
- [ ] **M-24** `UdpSender` поверх `ktor-network`, полная изоляция ошибок: ни одно исключение
      агента не всплывает в pipeline целевого сервиса.

## M3 — сервер: приём и хранение

- [ ] **M-30** Скелет по образцу katcher: Ktor CIO native, `ktor-server-di`, `ServerConfig` из env.
- [ ] **M-31** Схема SQLite + миграции (`PRAGMA user_version`, как
      `katcher/server/src/commonMain/.../db/Migrate.kt`).
- [ ] **M-32** UDP-приёмник: `aSocket(selector).udp().bind()`, разбор, валидация (`v`, `k`, `t`),
      автосоздание сервиса по новому `s`, батч-запись окон одной транзакцией.
- [ ] **M-33** Детект потерь: `w`, `q`/`n` → флаг `partial`, пропущенные окна, `clock_skew`.
- [ ] **M-36** Слияние инстансов на записи (read-modify-write в той же транзакции) +
      идемпотентность по `(s, i, t, q)`. **Тест на повторный пакет обязателен** — без него удвоение
      цифр не заметит никто.
- [ ] **M-37** Отметки деплоя: смена `rel` → строка в `deploys`.
- [ ] **M-34** Внутренние счётчики ingest + `/api/self`.
- [ ] **M-35** Нагрузочная проверка приёмника: 50 сервисов × 10 инстансов, потери и CPU.

## M4 — сервер: чтение

- [ ] **M-40** Эндпоинты [endpoint-query](docs/api/endpoint-query.md): `/services`, `/overview`,
      `/timeseries`, `/routes`, `/system`, `/slow`, `/deploys`.
- [ ] **M-41** Выбор источника по шагу (минуты / часы / дни) прозрачно для клиента; глубже 48 часов
      минуток нет — отдавать часовые и помечать это в ответе.
- [ ] **M-42** Auth по заголовкам reverse proxy (`X-Auth-Request-User`), `401` без них.
- [ ] **M-43** Админский контур: удаление сервиса (в т.ч. заведённого по опечатке),
      чтение и переопределение порогов алертов на сервис.

## M5 — дашборд

- [ ] **M-50** Compose SPA (`:composeApp`, wasm + desktop для отладки) + nginx-контейнер,
      ktor-client к `/api`.
- [ ] **M-51** Экраны: обзор, сервис (с отметками деплоев), маршруты, медленные, система, алерты.
- [ ] **M-52** Честность отображения: разрывы на `partial`/пропусках, подпись «перцентили
      приблизительные», «нет данных» вместо нулей, «RSS» вместо «heap» у нативных сервисов.

## M6 — алертинг

- [ ] **M-60** Правила `error_rate`, `latency`, `memory`, `absent` с порогами из конфига.
      `absent` — по сервису, не по инстансу (иначе алерт на каждый rolling update).
- [ ] **M-61** Машина состояний OK/FIRING: гистерезис, cooldown, нотификация на переходах.
- [ ] **M-62** Telegram-нотификатор (ktor-client CIO), устойчивый к ошибкам доставки;
      чат по умолчанию + переопределение на сервис.
- [ ] **M-63** Переопределение порогов на сервис (`alert_rules` с непустым `service_id`) + форма в UI.

## M7 — эксплуатация

- [ ] **M-70** Роллапы час/день (не трогая окна моложе 5 минут) и retention с `incremental_vacuum`;
      чистка инстансов без данных дольше суток.
- [ ] **M-71** Dockerfile (нативный бинарь) + Helm chart по образцу `katcher/charts/katcher`,
      включая `Service` с `protocol: UDP`.
- [ ] **M-72** README: подключение агента за 5 минут, требование reverse proxy перед UI,
      предупреждение «ingest-порт не выставлять наружу».
- [ ] **M-74** `LICENSE` до первой публикации образа. Проект открытый, но у katcher файла лицензии
      нет — то есть формально «все права защищены» и использовать его нельзя; здесь эту ошибку
      не повторяем. Выбор (Apache-2.0 / MIT) за владельцем.
- [ ] **M-73** Dogfooding: metrik мониторит сам себя и katcher (обе цели — нативные,
      это и есть проверка того, ради чего агент делался KMP).

## Отложено сознательно

* **Бинарный кодек** вместо JSON — когда появится сервис, которому 1200 байт на пакет мало
  (research §Р3).
* **HTTP-фолбэк транспорта** для агентов вне кластера (research, открытый вопрос 2).
* **SSE/WebSocket real-time** — минутное окно делает это бессмысленным (research §Р6);
  нативные артефакты `ktor-server-sse` есть, дверь открыта.
* **Пользовательские метрики** (`counter("orders")`) — не в «глупом» агенте v1.
* **Трейсинг и correlation id** — другой продукт.
* **Мультитенантность** — решено, что её нет: одна инсталляция = одна команда (research §Р8).
  Нужна изоляция — поднимается вторая инсталляция.
